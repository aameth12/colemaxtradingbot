# ATradeBot — Python dependencies
# Broker: ATrade | Assets: XAUUSD, AAPL, TSLA, NVDA, AMZN
# Timeframes: M5, M15 | Bridge: ZeroMQ (MT4 <-> Python)

# HTTP and data
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
pandas>=2.1.0
numpy>=1.26.0

# Market data (free — no API key required)
yfinance>=0.2.36

# Technical indicators (free — pure Python / pandas)
pandas_ta>=0.3.14b

# Telegram notifications (job-queue extra pulls in APScheduler for periodic jobs)
python-telegram-bot[job-queue]>=20.7

# WebSocket (live price feeds / broker WS API)
websocket-client>=1.7.0

# Task scheduling
schedule>=1.2.1

# Dashboard (Flask for lightweight REST, Dash for reactive UI — both included)
flask>=3.0.0
dash>=2.16.0

# MT4 bridge via ZeroMQ (EA uses DWX_ZeroMQ_Connector on the MT4 side)
pyzmq>=25.1.2

# Environment variable management
python-dotenv>=1.0.0

# Windows timezone database (required by ZoneInfo on Windows)
tzdata>=2024.1

# Logging with color support
loguru>=0.7.2

# MetaTrader 5 terminal bridge (Windows only — uncomment if running on Windows)
# MetaTrader5>=5.0.45
